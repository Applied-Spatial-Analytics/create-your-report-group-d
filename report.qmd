---
title: "Multi-Scale Spatial Analysis for Nature-Based Urban Heat Island Mitigation"
subtitle: "ARFW0501 – Group D Project Report"
author:
  - name: "Artemi Kurski"
    affiliation: "MSc Geomatics, TU Delft"
  - name: "Niranjan Pradeep"
    affiliation: "MSc Geomatics, TU Delft"
  - name: "Darius Floroiu"
    affiliation: "MSc Geomatics, TU Delft"
  - name: "Henryk Gujda"
    affiliation: "MSc Geomatics, TU Delft"
format:
  html:
    toc: true
    toc-depth: 3
    code-fold: true
    embed-resources: true
    theme: cosmo
---

```{=html}


<style>
/* ── Report-wide design tokens ────────────────────────── */
:root {
  --brand-blue:   #185FA5;
  --brand-teal:   #0F6E56;
  --brand-warm:   #BA7517;
  --brand-heat:   #A32D2D;
  --brand-green:  #3B6D11;
  --neutral-100:  #F1EFE8;
  --neutral-300:  #B4B2A9;
  --neutral-700:  #444441;
  font-family: "Source Sans Pro", "Helvetica Neue", sans-serif;
}

/* ── Callout / note boxes ─────────────────────────────── */
.note-box {
  border-left: 3px solid var(--brand-blue);
  background: #E6F1FB22;
  padding: 0.75rem 1rem;
  border-radius: 0 6px 6px 0;
  margin: 1.25rem 0;
  font-size: 0.9rem;
  color: #2c3e50;
}

/* ── Map figure wrapper ───────────────────────────────── */
.map-figure {
  margin: 1.5rem 0;
  border: 0.5px solid #d3d1c7;
  border-radius: 8px;
  overflow: hidden;
}
.map-figure img {
  width: 100%;
  display: block;
}
.map-caption {
  padding: 0.5rem 0.9rem;
  background: #F1EFE8;
  font-size: 0.8rem;
  color: #5F5E5A;
  border-top: 0.5px solid #d3d1c7;
}
.map-caption strong { color: #2c3e50; }

/* ── Cluster badge pills ──────────────────────────────── */
.cluster-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 12px;
  margin: 1.25rem 0;
}
.cluster-card {
  border-radius: 8px;
  padding: 0.85rem 1rem;
  font-size: 0.85rem;
  border: 0.5px solid transparent;
}
.cluster-card .label {
  font-weight: 600;
  font-size: 0.8rem;
  letter-spacing: 0.03em;
  margin-bottom: 4px;
}
.cc-1 { background:#EAF3DE; border-color:#639922; color:#27500A; }
.cc-2 { background:#FAEEDA; border-color:#BA7517; color:#633806; }
.cc-3 { background:#E6F1FB; border-color:#185FA5; color:#0C447C; }
.cc-4 { background:#FCEBEB; border-color:#A32D2D; color:#791F1F; }

/* ── Interactive widget shell ─────────────────────────── */
.widget-shell {
  border: 0.5px solid #d3d1c7;
  border-radius: 10px;
  padding: 1.25rem;
  margin: 1.5rem 0;
  background: #fafaf8;
}
.widget-shell h4 {
  margin: 0 0 0.75rem;
  font-size: 0.9rem;
  color: var(--neutral-700);
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.06em;
}
.slider-row {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 10px;
  font-size: 0.85rem;
  color: #5F5E5A;
}
.slider-row input[type=range] { flex: 1; }
.slider-val {
  min-width: 42px;
  text-align: right;
  font-weight: 600;
  color: var(--neutral-700);
}
.stat-row {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 8px;
  margin-top: 1rem;
}
.stat-cell {
  background: white;
  border: 0.5px solid #d3d1c7;
  border-radius: 6px;
  padding: 0.6rem 0.75rem;
  text-align: center;
}
.stat-cell .sv { font-size: 1.4rem; font-weight: 600; }
.stat-cell .sl { font-size: 0.72rem; color: #888780; margin-top: 2px; }

/* ── Workflow diagram table ───────────────────────────── */
.workflow-step {
  display: flex;
  align-items: flex-start;
  gap: 1rem;
  margin-bottom: 1.1rem;
}
.step-num {
  min-width: 28px;
  height: 28px;
  border-radius: 50%;
  background: var(--brand-blue);
  color: white;
  font-size: 0.8rem;
  font-weight: 700;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  margin-top: 2px;
}
.step-body { font-size: 0.9rem; line-height: 1.55; }
.step-body strong { color: #2c3e50; }

/* ── File-tree block ──────────────────────────────────── */
.file-tree {
  background: #2c3e50;
  color: #ecf0f1;
  border-radius: 8px;
  padding: 1rem 1.25rem;
  font-family: "Fira Mono", "Courier New", monospace;
  font-size: 0.78rem;
  line-height: 1.7;
  overflow-x: auto;
  margin: 1.25rem 0;
}
.ft-dir   { color: #74b9ff; font-weight: 600; }
.ft-py    { color: #55efc4; }
.ft-r     { color: #fd79a8; }
.ft-data  { color: #fdcb6e; }
.ft-other { color: #b2bec3; }


.cluster-card-full {
  width: 100%;
  border-radius: 10px;
  padding: 1.4rem 1.6rem;
  font-size: 1rem;
  border-left: 6px solid #e81710;
  background: #cbe2f822;
  color: #2c3e50;
  font-weight: 600;
  line-height: 1.5;
}
</style>
```

# Introduction

In the context of rapid urbanisation and climate change, it is increasingly important to understand how cities shape their local climate.

Comparing cities helps us see how differences in urban form influence temperature, environmental quality, and overall liveability. Cities differ in density, land use, building materials, and the amount of green space they contain. These factors strongly affect how heat is absorbed, stored, and released, which leads to noticeable temperature differences between urban areas. A key concept here is the **Urban Heat Island (UHI)** effect, where cities become warmer than their surrounding rural areas. This happens because materials like concrete and asphalt absorb and hold heat, vegetation is limited and cannot provide enough cooling through shade, and human activities such as transport, buildings, and industry release additional heat. Together, these factors increase local temperatures and can lead to higher heat stress, energy use, and air pollution.

Understanding UHI highlights the direct relationship between urban design and local climate. In this context, we aim to investigate how spatial data resolution influences UHI estimation and how urban blue-green infrastructure contributes to temperature mitigation.
In light of this we aim to explore the following research question:

```{=html}
<div class="cluster-card cluster-card-full">
  How can multi-scale spatial analysis be used to identify locations where nature-based solutions can most effectively mitigate urban heat island effects and what nature-based solutions are suitable on street level ?
</div>
```

::: {.note-box}
**City Selection:** 
To answer the research question, we focus on two case study cities: Rotterdam and Guangzhou. These cities were selected for their similarities in coastal location and economic significance, while also exhibiting distinct urban forms and climates. `Rotterdam`, located in the Netherlands, is known for its modern architecture and extensive port facilities, while `Guangzhou`, in southern China, features a mix of traditional and contemporary urban development. By comparing these two cities, we can gain insights into how different urban designs and green infrastructure strategies impact UHI intensity across varying spatial resolutions.
:::
---

## Methods

Our methodology relies on a dual - scale approach to understand the urban climate. We analyzed both macro - level administrative boundaries (Dutch *buurten* and Chinese *jiedao*) and micro - level street networks. By sticking entirely to open-source data—primarily OpenStreetMap (OSM) and Landsat satellite imagery, we ensured that the pipeline built for Rotterdam could be directly transferred to Guangzhou without coordinate or data availability issues.

# Getting started

The first step of the analysis involves identifying the required datasets and defining the spatial boundaries of the study area.

For this purpose, **Google Earth Engine (GEE)** was used to access and process the main datasets. These datasets form the basis for the initial setup and help in selecting the relevant neighbourhoods for further analysis.


**1. Land Surface Temperature (LST):**
 LST was derived from Landsat 8 data in Google Earth Engine. Thermal bands were processed to obtain surface temperature, which was then used to study spatial heat variations across Rotterdam.

**2. NDVI:**
 Vegetation cover was computed from Landsat 8 spectral bands in Google Earth Engine using the Normalized Difference Vegetation Index (NDVI), providing a measure of urban greenness.

**3. Local Climate Zones (LCZ):**
 Urban form classification was obtained using the RUBCLIM LCZ dataset available in Google Earth Engine, which categorises urban areas based on built type and land cover characteristics.

**4. Administrative boundaries:**
 Municipal and neighbourhood boundaries for Rotterdam were taken from the `CBS Wijken en Buurten 2025 GeoPackage`, used to define the spatial extent of the study area.

**5. Street network:**
 The pedestrian street network was extracted from OpenStreetMap using the `osmnx` library, and used for analysing urban connectivity and spatial structure.


# Analysis Workflow

```{=html}
<div class="widget-shell">
<h4>Pipeline overview</h4>

<div class="workflow-step">
  <div class="step-num">1</div>
  <div class="step-body"><strong>Data extraction</strong> — Raster data (LCZ, LST, NDVI) and vector data (administrative units, roads) were extracted to form the basis of all subsequent spatial analysis.</div>
</div>

<div class="workflow-step">
  <div class="step-num">2</div>
  <div class="step-body"><strong>Process data</strong> — Land Surface Temperature was normalised to enable consistent comparison across neighbourhoods and time periods.</div>
</div>

<div class="workflow-step">
  <div class="step-num">3</div>
  <div class="step-body"><strong>Computing vectors</strong> — At the neighbourhood level, mean LST, mean NDVI, and the dominant LCZ class were computed. At the street level, Pedestrian Space Temperature (PST), LST, NDVI, and LCZ were calculated per road segment.</div>
</div>

<div class="workflow-step">
  <div class="step-num">4</div>
  <div class="step-body"><strong>Identify hot neighbourhoods</strong> — Neighbourhoods showing distinct differences in LST, LCZ, and NDVI relative to the rest of the city were identified as thermal hotspots for further analysis.</div>
</div>

<div class="workflow-step">
  <div class="step-num">5</div>
  <div class="step-body"><strong>Street-segment clustering</strong> — Road segments within hot neighbourhoods were grouped into clusters using K-Means based on NDVI (vegetation), LCZ (urban form), and PST (pedestrian thermal exposure).</div>
</div>

<div class="workflow-step">
  <div class="step-num">6</div>
  <div class="step-body"><strong>Describe clusters</strong> — Clusters were characterised and ground-truthed through Google Street View imagery to verify thermal and land cover conditions on the ground.</div>
</div>

<div class="workflow-step">
  <div class="step-num">7</div>
  <div class="step-body"><strong>Propose nature-based solutions</strong> — Based on cluster characteristics, targeted nature-based solutions were proposed per street segment type to address heat exposure.</div>
</div>

<div class="workflow-step">
  <div class="step-num">8</div>
  <div class="step-body"><strong>Guangzhou comparison</strong> — The full pipeline was reproduced for Guangzhou and a comparative analysis of LCZ, PST, and mean NDVI was conducted between the two cities.</div>
</div>

</div>
```

## Typology construction

Instead of designing climate adaptations for thousands of individual streets or dozens of neighborhoods one by one, we grouped the urban fabric into distinct "typologies." These are essentially microclimate personas: clusters of areas that share the same environmental and spatial DNA. If we know what works for a specific typology in Rotterdam, we can test if that same solution works for its twin typology in Guangzhou.

We constructed these typologies across two distinct spatial scales:

**1. Data Acquisition and Indicator Processing**  
First, we established the thermal and physical baseline of the cities. Using Google Earth Engine, we pulled summer 2025 data from Landsat 8 (30m resolution) to calculate two key metrics:

*   **Vegetation (NDVI):** To measure the density of green infrastructure.
*   **Land Surface Temperature (LST):** To map the thermal landscape. To ensure accuracy across different satellite capture dates, we normalized the LST data by calculating the deviation from the spatial mean of each image, giving us a clean look at localized heat anomalies rather than day-to-day weather spikes. 

For urban morphology, we integrated the RUBCLIM Local Climate Zone (LCZ) dataset (100m resolution), which categorizes the physical build of the city into classes ranging from "Compact High-Rise" to "Sparsely Built."

**2. Pedestrian Accessibility (PST Engine)**  
To figure out how human-friendly these environments actually are, we couldn't just look at satellite pixels; we had to simulate how people move. Using OSM walking networks, we built a Python-based graph network to calculate Pedestrian Space Syntax (PST) accessibility. 

We identified "cooling hubs" (neighborhoods with an NDVI above the city median) and mapped their center points. The algorithm then simulated human navigation, counting the number of topological turns required to reach these cooling zones from any point on the street network. Paths requiring fewer than three turns received higher accessibility scores, mathematically penalizing areas that are cut off from blue-green infrastructure. 

**3. Macro-Scale Clustering (Neighborhoods)**  
With our variables set (Mean LST, Mean NDVI, LCZ class, and PST score), we aggregated the data up to the neighborhood level using zonal statistics. Because our data was mixed (blending continuous numbers like temperature with categorical tags like LCZ codes), standard clustering methods wouldn't work. 

Instead, we ran the data through R using a Gower distance matrix, which is specifically designed to handle mixed data types. We then applied Partitioning Around Medoids (PAM) clustering to group the neighborhoods into four distinct microclimate profiles.

**4. Micro-Scale Clustering (Street Segments)**  
Finally, we brought the analysis down to the human scale to find the most vulnerable individual streets. Using Python and Geopandas, we slightly buffered the OSM street linestrings and extracted the exact LST and NDVI pixel values sitting directly under the pavement, while sampling the LCZ classification at the start point of each street. 

After dealing with missing values and standardizing the numeric data, we used an elbow plot to determine the optimal number of clusters and ran a MiniBatch K-Means clustering algorithm. To verify that our math matched physical reality, the pipeline randomly sampled 50 streets from our most extreme clusters and exported their coordinates to a CSV, allowing us to drop into Google Street View and qualitatively inspect the physical characteristics of the hottest streets in the city.

```{r message=FALSE, warning=FALSE}
library(sf)
library(dplyr)
library(plotly)

# Read the layer from the GeoPackage
invisible(capture.output(
  roads_data <- st_read("data/rotterdam/rotterdam_roads_clustered.gpkg",
                        layer = "rotterdam_roads_data")
))

# Randomly select 500 rows
set.seed(42)  # remove or change this if you don't want reproducible sampling
roads_sample <- roads_data %>% slice_sample(n = 500)

# Colors for clusters 0-5
cluster_colors <- c(
  "0" = "#4C72B0",
  "1" = "#65C3A1",
  "2" = "#FC9964",
  "3" = "#869DC5",
  "4" = "#E597C0",
  "5" = "#D9C04C"
)

cluster3d_scaled <- plot_ly(
  x = roads_sample$lst,
  y = roads_sample$ndvi,
  z = roads_sample$Bww1kM,
  type = "scatter3d",
  mode = "markers",
  color = as.factor(roads_sample$cluster),
  colors = cluster_colors,
  marker = list(size = 3)
) %>%
  layout(
    title = "Street clusters in Rotterdam",
    scene = list(
      xaxis = list(title = "LST"),
      yaxis = list(title = "NDVI"),
      zaxis = list(title = "Betweenness")
    )
  )
cluster3d_scaled
```


## Small-scale output: Rotterdam

After completing step 1 and 2, the following results were obtained, which formed the basis for further analysis.

```{=html}
<div class="map-figure">
  <img src="Report data/Images/Rotterdam/rotterdam_ndvi_map.png"
       alt="Map of Rotterdam showing mean NDVI vegetation index distribution per neighbourhood." />
  <div class="map-caption">
    <strong>Figure 1.</strong> Rotterdam NDVI Vegetation Index Distribution
  </div>
</div>

<div class="map-figure">
  <img src="Report data/Images/Rotterdam/rotterdam_lst_map.png"
       alt="Map of Rotterdam showing land surface temperature per neighbourhood. Orange and yellow tones indicate higher LST in the city core and port zones" />
  <div class="map-caption">
    <strong>Figure 2.</strong> Rotterdam Land Surface Temperature (LST) 
  </div>
</div>

<div class="map-figure">
  <img src="Report data/Images/Rotterdam/rotterdam_lcz_map_official.png"
       alt="Map of Rotterdam showing dominant Local Climate Zone class per neighbourhood using WMO standard colours." />
  <div class="map-caption">
    <strong>Figure 3.</strong> Rotterdam Local Climate Zone Layouts 
  </div>
</div>
```


From the analysis, three neighbourhoods stand out as showing extreme variation relative to the rest of the city: **Bloemhof, Afrikaanderwijk, and Hillesluis**.

```{=html}
<div class="map-figure">
<div style="display: flex; align-items: flex-start; gap: 32px; padding: 1rem 0;">

  <img src="Report data/Images/Rotterdam/Street-wise-clustering.png" alt="Street-wise clustering map" style="width: 65%; border-radius: 8px;">

  <div style="flex: 1;">
    <h3 style="font-size: 16px; font-weight: 500; color: var(--color-text-primary); margin: 0 0 4px;">Street-wise clustering</h3>
    <p style="font-size: 13px; font-weight: 500; color: var(--color-text-secondary); margin: 0 0 20px;">relating LST, NDVI and PST</p>

    <div style="display: flex; flex-direction: column; gap: 10px;">

      <div style="display: flex; align-items: center; gap: 10px;">
        <span style="width: 12px; height: 12px; border-radius: 50%; background: #8ab4e8; flex-shrink: 0;"></span>
        <span style="font-size: 13px; color: var(--color-text-secondary);">0. &nbsp; High LST &nbsp;/&nbsp; Low NDVI &nbsp;/&nbsp; Elevated PST</span>
      </div>

      <div style="display: flex; align-items: center; gap: 10px;">
        <span style="width: 12px; height: 12px; border-radius: 50%; background: #c9b84c; flex-shrink: 0;"></span>
        <span style="font-size: 13px; color: var(--color-text-secondary);">1. &nbsp; Medium LST &nbsp;/&nbsp; Medium NDVI &nbsp;/&nbsp; High PST</span>
      </div>

      <div style="display: flex; align-items: center; gap: 10px;">
        <span style="width: 12px; height: 12px; border-radius: 50%; background: #4caf72; flex-shrink: 0;"></span>
        <span style="font-size: 13px; color: var(--color-text-secondary);">2. &nbsp; Low LST &nbsp;/&nbsp; High NDVI &nbsp;/&nbsp; Low PST</span>
      </div>

      <div style="display: flex; align-items: center; gap: 10px;">
        <span style="width: 12px; height: 12px; border-radius: 50%; background: #2a5fa8; flex-shrink: 0;"></span>
        <span style="font-size: 13px; color: var(--color-text-secondary);">3. &nbsp; Low LST &nbsp;/&nbsp; Low NDVI &nbsp;/&nbsp; High PST</span>
      </div>

      <div style="display: flex; align-items: center; gap: 10px;">
        <span style="width: 12px; height: 12px; border-radius: 50%; background: #e87bb0; flex-shrink: 0;"></span>
        <span style="font-size: 13px; color: var(--color-text-secondary);">4. &nbsp; Medium LST &nbsp;/&nbsp; Medium NDVI &nbsp;/&nbsp; High PST</span>
      </div>

      <div style="display: flex; align-items: center; gap: 10px;">
        <span style="width: 12px; height: 12px; border-radius: 50%; background: #e8824a; flex-shrink: 0;"></span>
        <span style="font-size: 13px; color: var(--color-text-secondary);">5. &nbsp; Medium LST &nbsp;/&nbsp; Medium NDVI &nbsp;/&nbsp; Low PST</span>
      </div>

    </div>
  </div>

</div>
</div>
```















## Small-scale output: Guangzhou
```{=html}
<div class="map-figure">
  <img src="Report data/Images/Guangzhou/NDVI_guangzhou.jpeg"
       alt="Map of Guangzhou showing mean NDVI vegetation index distribution per neighbourhood." />
  <div class="map-caption">
    <strong>Figure 4.</strong> Guangzhou NDVI Vegetation Index Distribution  
  </div>
</div>

<div class="map-figure">
  <img src="Report data/Images/Guangzhou/LST_guangzhou.jpeg"
       alt="Map of Guangzhou showing land surface temperature per neighbourhood. Orange and yellow tones indicate higher LST in the city core and port zones" />
  <div class="map-caption">
    <strong>Figure 5.</strong> Guangzhou Land Surface Temperature (LST) 
  </div>
</div>

<div class="map-figure">
  <img src="Report data/Images/Guangzhou/NDVI_guangzhou.jpeg"
       alt="Map of Guangzhou showing dominant Local Climate Zone class per neighbourhood using WMO standard colours." />
  <div class="map-caption">
    <strong>Figure 6.</strong> Guangzhou Local Climate Zone Layouts
  </div>
</div>
```





# Results

## Rotterdam: Street-view and Nature based solutions

```{=html}
<style>
.map-container { position: relative; display: inline-block; }
.map-img { width: 200px; border-radius: 8px; display: block; }
.dot { position: absolute; width: 14px; height: 14px; border: 2px solid white; border-radius: 50%; animation: pulse 2s infinite; cursor: pointer; transition: transform 0.2s; transform: translate(-50%, -50%); }
.dot:hover { transform: translate(-50%, -50%) scale(1.3); }
.dot.active { outline: 3px solid white; }
@keyframes pulse { 0% { box-shadow: 0 0 0 0 rgba(255,255,255,0.4); } 70% { box-shadow: 0 0 0 10px rgba(255,255,255,0); } 100% { box-shadow: 0 0 0 0 rgba(255,255,255,0); } }
.sv-card { display: none; position: relative; border-radius: 8px; overflow: hidden; }
.sv-card.visible { display: block; }
.sv-card img { width: 100%; border-radius: 8px; display: block; aspect-ratio: 16/9; object-fit: cover; }
.sv-card .sv-label { position: absolute; bottom: 0; left: 0; right: 0; background: rgba(0,0,0,0.6); color: #fff; font-size: 11px; padding: 4px 8px; }
.info-panel { display: none; margin-top: 14px; }
.info-panel.visible { display: flex; align-items: flex-start; gap: 16px; }
.info-text { flex: 1; padding: 14px 16px; border-radius: 10px; border: 1px solid var(--color-border-tertiary); background: var(--color-background-secondary); }
.info-text h3 { font-size: 15px; font-weight: 500; color: var(--color-text-primary); margin: 0 0 8px; }
.info-text ul { font-size: 14px; color: var(--color-text-secondary); line-height: 2; padding-left: 1rem; margin: 0; }
.info-ref-img { width: 240px; flex-shrink: 0; border-radius: 10px; object-fit: cover; aspect-ratio: 9/16; }
.hint { font-size: 12px; color: var(--color-text-tertiary); margin-top: 6px; }
.module-box { border: 1px solid #ccc; border-radius: 12px; padding: 20px; }
.module-title { font-size: 24px; font-weight: 1000; color: var(--color-text-primary); margin: 0 0 16px; }
</style>

<div class="module-box">

  <p class="module-title">Street-level observations</p>

  <div style="display: flex; flex-direction: column; align-items: center; gap: 16px;">

    <div>
      <div class="map-container">
        <img class="map-img" src="Report data/Images/Rotterdam/street-views/neighbourhood.png" alt="Neighbourhood map">
        <div class="dot" style="top: 75%; left: 77%; background: #2ecc71;" onclick="selectDot(0, this)" title="Vlasakkersstraat"></div>
        <div class="dot" style="top: 45%; left: 45%; background: #e74c3c;" onclick="selectDot(1, this)" title="Anemoonstraat"></div>
        <div class="dot" style="top: 30%; left: 20%; background: #3498db;" onclick="selectDot(2, this)" title="Jouberstraat"></div>
      </div>
      <p class="hint" style="text-align: center;">Click a dot to see street view</p>
    </div>

    <div id="sv-panel" style="display: none; width: 100%;">
      <div class="sv-card" id="card-0">
        <img src="Report data/Images/Rotterdam/street-views/street-1.png" alt="Street view 1">
        <div class="sv-label">📍 Vlasakkersstraat</div>
      </div>
      <div class="sv-card" id="card-1">
        <img src="Report data/Images/Rotterdam/street-views/street-2.png" alt="Street view 2">
        <div class="sv-label">📍 Anemoonstraat</div>
      </div>
      <div class="sv-card" id="card-2">
        <img src="Report data/Images/Rotterdam/street-views/street-3.png" alt="Street view 3">
        <div class="sv-label">📍 Jouberstraat</div>
      </div>
    </div>

    <div class="info-panel" id="info-panel" style="width: 100%;">
      <img id="info-ref-img" class="info-ref-img" src="" alt="Reference image">
      <div class="info-text">
        <h3 id="info-title"></h3>
        <ul id="info-list"></ul>
      </div>
    </div>

  </div>
</div>

<script>
const streetInfo = [
  {
    title: "Vlasakkersstraat — Green facades",
    img: "Report data/Images/Rotterdam/street-views/solutions/green-facade.png",
    points: [
      "Limited space available for ground-level planting",
      "Increased insulation of building facades",
      "Reduces surface temperature and air pollution"
    ]
  },
  {
    title: "Anemoonstraat — Street trees",
    img: "Report data/Images/Rotterdam/street-views/solutions/free-standing-wall.png",
    points: [
      "Wide pavement allows tree pit installation",
      "Provides shade and reduces pedestrian heat stress",
      "Improves stormwater absorption"
    ]
  },
  {
    title: "Jouberstraat — Permeable paving",
    img: "Report data/Images/Rotterdam/street-views/solutions/group-of-trees.png",
    points: [
      "High impervious surface coverage",
      "Permeable pavers reduce surface runoff",
      "Supports evaporative cooling at street level"
    ]
  }
];

let activeDot = null;

function selectDot(index, dotEl) {
  if (activeDot) activeDot.classList.remove('active');
  activeDot = dotEl;
  dotEl.classList.add('active');

  document.querySelectorAll('.sv-card').forEach((c, i) => {
    c.classList.toggle('visible', i === index);
  });

  document.getElementById('sv-panel').style.display = 'block';

  const info = streetInfo[index];
  document.getElementById('info-title').textContent = info.title;
  document.getElementById('info-list').innerHTML = info.points.map(p => `<li>${p}</li>`).join('');
  document.getElementById('info-ref-img').src = info.img;
  document.getElementById('info-panel').classList.add('visible');
}
</script>
```


## Gunagzhou: Street-view and Nature based solutions





```{=html}
<div class="widget-shell">

<h4 style="text-align:center;">Neighbourhood Analysis</h4>

<div style="display:flex; justify-content:center; gap:10px; margin:15px 0; flex-wrap:wrap;">

  <button id="lstBtn"
    style="
      padding:10px 18px;
      border:none;
      border-radius:8px;
      background:#A32D2D;
      color:white;
      font-weight:600;
      cursor:pointer;">
    Heat Island (LST)
  </button>

  <button id="ndviBtn"
    style="
      padding:10px 18px;
      border:1px solid #d3d1c7;
      border-radius:8px;
      background:white;
      font-weight:600;
      cursor:pointer;">
    Vegetation (NDVI)
  </button>

</div>

<!-- LST PANEL -->
<div id="lstPanel" style="text-align:center;">

  <h5>Top Heat Island Neighbourhoods</h5>

  <div style="display:inline-block; text-align:left;">

    <div class="workflow-step">
      <div class="step-num" style="background:#A32D2D;">1</div>
      <div class="step-body"><strong>Bedrijvenpark Noord-West</strong> — +10.60°C</div>
    </div>

    <div class="workflow-step">
      <div class="step-num" style="background:#B23A3A;">2</div>
      <div class="step-body"><strong>Spaanse Polder</strong> — +9.18°C</div>
    </div>

    <div class="workflow-step">
      <div class="step-num" style="background:#C24A4A;">3</div>
      <div class="step-body"><strong>Zuidplein</strong> — +8.50°C</div>
    </div>

    <div class="workflow-step">
      <div class="step-num" style="background:#D35D5D;">4</div>
      <div class="step-body"><strong>Nieuw Mathenesse</strong> — +8.02°C</div>
    </div>

    <div class="workflow-step">
      <div class="step-num" style="background:#E07474;">5</div>
      <div class="step-body"><strong>Waalhaven Zuid</strong> — +7.85°C</div>
    </div>

  </div>
</div>

<!-- NDVI PANEL -->
<div id="ndviPanel" style="display:none; text-align:center;">

  <h5>Top Vegetation Deficit Neighbourhoods</h5>

  <div style="display:inline-block; text-align:left;">

    <div class="workflow-step">
      <div class="step-num" style="background:#3B6D11;">1</div>
      <div class="step-body"><strong>Kop van Zuid</strong> — −0.230 NDVI</div>
    </div>

    <div class="workflow-step">
      <div class="step-num" style="background:#4A7F1A;">2</div>
      <div class="step-body"><strong>Cs Kwartier</strong> — −0.253 NDVI</div>
    </div>

    <div class="workflow-step">
      <div class="step-num" style="background:#5A9422;">3</div>
      <div class="step-body"><strong>Zuidplein</strong> — −0.268 NDVI</div>
    </div>

    <div class="workflow-step">
      <div class="step-num" style="background:#6AA82B;">4</div>
      <div class="step-body"><strong>Waalhaven Zuid</strong> — −0.274 NDVI</div>
    </div>

    <div class="workflow-step">
      <div class="step-num" style="background:#7BBC35;">5</div>
      <div class="step-body"><strong>Spaanse Polder</strong> — −0.281 NDVI</div>
    </div>

  </div>
</div>

<div class="map-caption" style="text-align:center;">
<strong>Key Insight.</strong> Heat hotspots align strongly with industrial zones, while vegetation deficits cluster in highly built-up or infrastructurally dense neighbourhoods.
</div>

</div>

<script>
const lstBtn = document.getElementById("lstBtn");
const ndviBtn = document.getElementById("ndviBtn");

const lstPanel = document.getElementById("lstPanel");
const ndviPanel = document.getElementById("ndviPanel");

function setActive(btn, activeColor) {
  btn.style.background = activeColor;
  btn.style.color = "white";
  btn.style.border = "none";
}

function setInactive(btn) {
  btn.style.background = "white";
  btn.style.color = "black";
  btn.style.border = "1px solid #d3d1c7";
}

lstBtn.onclick = () => {
  lstPanel.style.display = "block";
  ndviPanel.style.display = "none";

  setActive(lstBtn, "#A32D2D");
  setInactive(ndviBtn);
};

ndviBtn.onclick = () => {
  lstPanel.style.display = "none";
  ndviPanel.style.display = "block";

  setActive(ndviBtn, "#3B6D11");
  setInactive(lstBtn);
};
</script>
```

```{r message=FALSE, warning=FALSE}
library(sf)
library(dplyr)
library(plotly)

# Read the layer from the GeoPackage
invisible(capture.output(
  gzhou_roads_data <- st_read("data/guangzhou/guangzhou_roads_clustered.gpkg",
                        layer = "guangzhou_roads_data")
))

# Randomly select 500 rows
set.seed(42)  # remove or change this if you don't want reproducible sampling
gzhou_roads_sample <- gzhou_roads_data %>% slice_sample(n = 500)

# Colors for clusters 0-5
cluster_colors <- c(
  "0" = "#4C72B0",
  "1" = "#65C3A1",
  "2" = "#FC9964",
  "3" = "#869DC5",
  "4" = "#E597C0",
  "5" = "#D9C04C"
)

cluster3d_scaled <- plot_ly(
  x = gzhou_roads_sample$lst,
  y = gzhou_roads_sample$ndvi,
  z = gzhou_roads_sample$Bww1kM,
  type = "scatter3d",
  mode = "markers",
  color = as.factor(gzhou_roads_sample$cluster),
  colors = cluster_colors,
  marker = list(size = 3)
) %>%
  layout(
    title = "Street clusters in Guangzhou",
    scene = list(
      xaxis = list(title = "LST"),
      yaxis = list(title = "NDVI"),
      zaxis = list(title = "Betweenness")
    )
  )
cluster3d_scaled
```

```{r message=FALSE, warning=FALSE}
# LST/NDVI correlation for Gzhou

library(plotly)

# Correlation coefficient
correlation <- cor(gzhou_roads_sample$lst, gzhou_roads_sample$ndvi, use = "complete.obs")

# Linear model for the trend line
lm_fit <- lm(ndvi ~ lst, data = gzhou_roads_sample)
lst_range <- seq(min(gzhou_roads_sample$lst, na.rm = TRUE),
                  max(gzhou_roads_sample$lst, na.rm = TRUE),
                  length.out = 100)
trend_pred <- predict(lm_fit, newdata = data.frame(lst = lst_range))

scatter_lst_ndvi <- plot_ly(
  data = gzhou_roads_sample,
  x = ~lst,
  y = ~ndvi,
  type = "scatter",
  mode = "markers",
  color = ~as.factor(cluster),
  colors = cluster_colors,
  marker = list(size = 6, opacity = 0.7),
  name = ~as.factor(cluster)
) %>%
  add_trace(
    x = lst_range,
    y = trend_pred,
    type = "scatter",
    mode = "lines",
    line = list(color = "#000000", width = 2, dash = "dash"),
    name = "Trend line",
    inherit = FALSE   # <-- prevents it from inheriting the marker settings above
  ) %>%
  layout(
    title = paste0("Guangzhou: LST vs NDVI (r = ", round(correlation, 3), ")"),
    xaxis = list(title = "LST"),
    yaxis = list(title = "NDVI")
  )

scatter_lst_ndvi
```

```{r message=FALSE, warning=FALSE}
# LST/NDVI correlation for Rdam
library(plotly)

# Correlation coefficient
correlation <- cor(roads_sample$lst, roads_sample$ndvi, use = "complete.obs")

# Linear model for the trend line
lm_fit <- lm(ndvi ~ lst, data = roads_sample)
lst_range <- seq(min(roads_sample$lst, na.rm = TRUE),
                  max(roads_sample$lst, na.rm = TRUE),
                  length.out = 100)
trend_pred <- predict(lm_fit, newdata = data.frame(lst = lst_range))

scatter_lst_ndvi <- plot_ly(
  data = roads_sample,
  x = ~lst,
  y = ~ndvi,
  type = "scatter",
  mode = "markers",
  color = ~as.factor(cluster),
  colors = cluster_colors,
  marker = list(size = 6, opacity = 0.7),
  name = ~as.factor(cluster)
) %>%
  add_trace(
    x = lst_range,
    y = trend_pred,
    type = "scatter",
    mode = "lines",
    line = list(color = "#000000", width = 2, dash = "dash"),
    name = "Trend line",
    inherit = FALSE   # <-- prevents it from inheriting the marker settings above
  ) %>%
  layout(
    title = paste0("Rotterdam: LST vs NDVI (r = ", round(correlation, 3), ")"),
    xaxis = list(title = "LST"),
    yaxis = list(title = "NDVI")
  )

scatter_lst_ndvi
```

```{r message=FALSE, warning=FALSE}
library(sf)
library(dplyr)
library(plotly)

# Compute % of total segment length per cluster for one roads dataset
cluster_length_pct <- function(roads_data, all_clusters = names(cluster_colors)) {
  roads_data$seg_length <- as.numeric(st_length(roads_data))

  pct_df <- roads_data %>%
    st_drop_geometry() %>%
    mutate(cluster = as.character(cluster)) %>%
    group_by(cluster) %>%
    summarise(total_length = sum(seg_length, na.rm = TRUE), .groups = "drop") %>%
    mutate(pct = 100 * total_length / sum(total_length))

  tibble(cluster = all_clusters) %>%
    left_join(pct_df, by = "cluster") %>%
    mutate(pct = ifelse(is.na(pct), 0, pct))
}

# Build a grouped bar chart: cities on shared x-axis, bars colored by cluster
# data_list: named list, e.g. list(Rotterdam = roads_data, Guangzhou = gzhou_roads_data)
plot_cluster_bar <- function(data_list, colors = cluster_colors) {
  combined_df <- bind_rows(
    lapply(names(data_list), function(city_name) {
      cluster_length_pct(data_list[[city_name]]) %>%
        mutate(city = city_name)
    })
  )

  plot_ly(
    data = combined_df,
    x = ~city,
    y = ~pct,
    color = ~cluster,
    colors = colors,
    type = "bar"
  ) %>%
    layout(
      title = "% of total road segment length by cluster",
      xaxis = list(title = ""),
      yaxis = list(title = "% of total length", ticksuffix = "%"),
      barmode = "group",
      legend = list(title = list(text = "Cluster"))
    )
}

bar_chart <- plot_cluster_bar(
  list(Rotterdam = roads_data, Guangzhou = gzhou_roads_data)
)
bar_chart

```

# Discussion

# Conclusion
